const modeSwitch = document.getElementById('modeSwitch');
const body = document.body;

modeSwitch.addEventListener('change', () => {
    body.classList.toggle('dark-mode');
});
alert("Bem vindos ao meu projeto de estudo!")

